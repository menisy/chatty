<!DOCTYPE html>
<html>
  <head>
    <title>Chatty</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "faye" %>
    <%= javascript_include_tag "bootstrap.min" %>
    <%= csrf_meta_tags %>
    <script type="text/javascript">
      function CloseIt(){
      $.ajax({
              url:"<%= chats_logout_path %>",
              type:'POST',
              async: false,
              datatype:'json',
              traditional:true,
              data:{
              },
              timeout: 5000,
        });
      }
    </script>
  </head>
  <body>
    <div class="main-container">
      <%= yield %>
    </div>
  </body>
  <script type="text/javascript">
      var i;
      arr = ["#e5e5e5","#C8DCFF"];
      var n=25;
      for(i=0;i<n;i++){
        var width = parseInt($(window).width())+100;
        var height = parseInt($(window).height())+100;
        var x=Math.floor((Math.random()*width)-150);
        var y=Math.floor((Math.random()*height)-150);
        var w=Math.floor((Math.random()*400)+100);
        var h=Math.floor((Math.random()*400)+100);
        var l=-1;
        index = Math.floor(Math.random()*2);
        if(i>n/2)
          color = arr[0];
        else
          color = arr[1];
        colorb = color;
        if(color == "#e5e5e5")
          colorb = "#757575";
        var rad=Math.floor(h/2);
        $("body").append('<div class="bk" style="position:absolute;width:'+w+'px;height:'+h+'px;left:'+x+'px;top:'+y+'px;z-index:'+l+';background-color:transparent;border-radius:'+rad+'px;box-shadow: 1px 1px 20px '+colorb+';border:1px solid '+color+';"></div>');
        }
  </script>
</html>
